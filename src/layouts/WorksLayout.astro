---
import Layout from './Layout.astro';

const { frontmatter } = Astro.props;
const title = frontmatter?.title ? String(frontmatter.title) : null;
---
<Layout current="works" title={title}>
  <slot />
</Layout>